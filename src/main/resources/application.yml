openshift:
    config:
        predictor:
            url: http://predictor-jiminy2.e8ca.engint.openshiftapps.com/predictions/ranks
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
    driverClassName: org.postgresql.Driver
  jpa:
    generate-ddl: true
server:
    port: 8181
logging:
     path: /logs/jiminy-html-server
     file: server.log
     level:
         root: INFO
